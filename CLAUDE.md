# CLAUDE.md — adventureorno.com

> The commercial apps have **no decided name**. "Flok" was a working title and is NOT
> decided (Erica, 2026-08-15) — do not use it in the product, the docs or the repo.

> # 👉 READ [`docs/STATE.md`](docs/STATE.md). IT IS THE ONLY DOCUMENT.
>
> The plan, the data model, the business rules, the operations runbooks, the security
> baseline and the decision history are ALL in that one file. This file exists only
> because Claude Code loads it automatically at the start of a session — it is a
> pointer, not a second source of truth.
>
> **Never create another `.md`.** New instructions, decisions and plans are added to
> `docs/STATE.md`. Erica, 2026-08-11: *"it is now the SINGLE source of truth and any
> future changes and instructions should be added there, never create a new MD."*
>
> If a new direction conflicts with STATE.md: say what the change would do, ask whether
> that is what she wants, then write the decision into STATE.md.
